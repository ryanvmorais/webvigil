"""
Vendored Retire.js data files (see PROVENANCE.json).

Holds ``retirejs.json`` (the normalised advisory database) and
``PROVENANCE.json`` (where it came from and when). Loaded through
:mod:`importlib.resources`, never a path relative to the source tree.
"""
