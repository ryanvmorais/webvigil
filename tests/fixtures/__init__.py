"""
Test-target applications with deliberately (in)secure configurations.

:mod:`tests.fixtures.app` builds the Starlette app in an ``insecure`` or
``hardened`` profile; :mod:`tests.fixtures.serve` is the standalone ASGI entry
point for manual scans and demos.
"""
