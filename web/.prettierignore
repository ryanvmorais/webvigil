# --- 1. GENERATED AND VENDORED ---
# Not ours to format: build output, dependencies, lockfile.
.next
node_modules
pnpm-lock.yaml

# --- 2. TEST AND COVERAGE OUTPUT ---
coverage
playwright-report
test-results
.playwright-tmp

# --- 3. GENERATED CODE ---
# api-types.ts comes from `pnpm gen:api`; openapi.json from scripts/dump-openapi.py;
# next-env.d.ts from Next. Reformatting them only adds noise to the next regen.
src/lib/api-types.ts
openapi.json
next-env.d.ts
